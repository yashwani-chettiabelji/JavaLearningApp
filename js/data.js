"use strict";

window.APP_DATA = {"version":"2.0.0","topics":[{"id":"Variables","title":"Variables","summary":"Named containers for data — learn declaration, assignment, and naming rules.","content":"<p>Variables are named storage locations in memory. Every variable in Java has a <b>type</b>, a <b>name</b>, and optionally a <b>value</b>.</p>\n<h4>Declaration &amp; Assignment</h4>\n<ul>\n<li><b>Declaration:</b> <code>int age;</code> — reserves space, no value yet.</li>\n<li><b>Initialization:</b> <code>int age = 25;</code> — declare and assign in one line.</li>\n<li><b>Reassignment:</b> <code>age = 26;</code> — change the value later.</li>\n</ul>\n<div class=\"code-example\">int score = 100;\ndouble price = 19.99;\nString name = \"Alice\";\nboolean isActive = true;</div>\n<h4>Naming Rules</h4>\n<ul>\n<li>Start with a letter, <code>_</code>, or <code>$</code> (letters recommended).</li>\n<li>Cannot use Java reserved words like <code>class</code> or <code>int</code>.</li>\n<li>Use camelCase: <code>studentName</code>, <code>totalAmount</code>.</li>\n</ul>\n<p class=\"note-tip\"><b>Tip:</b> Declare variables as close as possible to where they are first used — it keeps code readable and limits scope.</p>"},{"id":"Data Types","title":"Data Types","summary":"Primitives, reference types, and how Java enforces type safety at compile time.","content":"<p>Java is <b>statically typed</b>: the compiler checks types before the program runs. Types fall into two categories.</p>\n<h4>Primitive Types (8 total)</h4>\n<ul>\n<li><code>byte</code> (8-bit) · <code>short</code> (16-bit) · <code>int</code> (32-bit) · <code>long</code> (64-bit)</li>\n<li><code>float</code> · <code>double</code> (floating-point decimals)</li>\n<li><code>char</code> (single Unicode character) · <code>boolean</code> (true/false)</li>\n</ul>\n<h4>Reference Types</h4>\n<p>Objects and arrays live on the heap; the variable holds a <b>reference</b> (memory address), not the object itself. Examples: <code>String</code>, <code>Integer</code>, arrays, and custom classes.</p>\n<div class=\"code-example\">int count = 42;           // primitive — value stored directly\nString city = \"London\";   // reference — points to a String object\nint[] nums = {1, 2, 3};   // reference to an array object</div>\n<p class=\"note-tip\"><b>Tip:</b> Use <code>int</code> for whole numbers and <code>double</code> for decimals by default. Pick smaller types only when memory is critical.</p>"},{"id":"Control Flow","title":"Control Flow","summary":"Branch and decide with if/else, switch, and the ternary operator.","content":"<p>Control flow lets your program make decisions and choose different paths based on conditions.</p>\n<h4>if / else if / else</h4>\n<div class=\"code-example\">int score = 85;\n\nif (score >= 90) {\n    System.out.println(\"Grade: A\");\n} else if (score >= 80) {\n    System.out.println(\"Grade: B\");\n} else {\n    System.out.println(\"Grade: C\");\n}</div>\n<h4>switch</h4>\n<p>Best for matching a single variable against many constant values. Since Java 14+, use arrow syntax for cleaner code:</p>\n<div class=\"code-example\">String day = \"Monday\";\n\nswitch (day) {\n    case \"Saturday\", \"Sunday\" -> System.out.println(\"Weekend!\");\n    case \"Monday\" -> System.out.println(\"Start of week\");\n    default -> System.out.println(\"Midweek\");\n}</div>\n<h4>Ternary Operator</h4>\n<p>A compact if/else: <code>condition ? valueIfTrue : valueIfFalse</code></p>\n<div class=\"code-example\">int max = (a > b) ? a : b;</div>\n<p class=\"note-tip\"><b>Tip:</b> Always use braces <code>{}</code> in if-blocks, even for single statements — it prevents bugs when you add lines later.</p>"},{"id":"Loops","title":"Loops","summary":"Repeat code with for, while, do-while, and enhanced for-each loops.","content":"<p>Loops repeat a block of code until a condition is met or a collection is exhausted.</p>\n<h4>for Loop</h4>\n<p>Ideal when you know how many iterations you need.</p>\n<div class=\"code-example\">for (int i = 0; i < 5; i++) {\n    System.out.println(\"Count: \" + i);\n}</div>\n<h4>while &amp; do-while</h4>\n<div class=\"code-example\">int n = 0;\nwhile (n < 3) {          // may run zero times\n    n++;\n}\n\ndo {                     // runs at least once\n    System.out.println(n);\n} while (n < 0);</div>\n<h4>Enhanced for-each</h4>\n<div class=\"code-example\">String[] fruits = {\"apple\", \"banana\", \"cherry\"};\nfor (String fruit : fruits) {\n    System.out.println(fruit);\n}</div>\n<h4>Loop Control</h4>\n<ul>\n<li><code>break</code> — exit the loop immediately.</li>\n<li><code>continue</code> — skip to the next iteration.</li>\n</ul>\n<p class=\"note-tip\"><b>Tip:</b> Avoid infinite loops — always ensure the loop condition eventually becomes false.</p>"},{"id":"Classes and Objects","title":"Classes & Objects","summary":"Blueprints and instances — fields, constructors, and the new keyword.","content":"<p>A <b>class</b> is a blueprint; an <b>object</b> is a specific instance created from that blueprint.</p>\n<h4>Defining a Class</h4>\n<div class=\"code-example\">public class Dog {\n    String name;          // field (state)\n    int age;\n\n    public Dog(String name, int age) {  // constructor\n        this.name = name;\n        this.age = age;\n    }\n\n    public void bark() {  // method (behavior)\n        System.out.println(name + \" says woof!\");\n    }\n}</div>\n<h4>Creating Objects</h4>\n<div class=\"code-example\">Dog buddy = new Dog(\"Buddy\", 3);\nbuddy.bark();  // Buddy says woof!</div>\n<h4>Key Terms</h4>\n<ul>\n<li><b>Field</b> — variable belonging to an object.</li>\n<li><b>Method</b> — function defined inside a class.</li>\n<li><b>Constructor</b> — special method called by <code>new</code> to initialize state.</li>\n<li><b><code>this</code></b> — refers to the current object instance.</li>\n</ul>\n<p class=\"note-tip\"><b>Tip:</b> One Java file per public class, and the filename must match the class name exactly (e.g. <code>Dog.java</code>).</p>"},{"id":"OOP Concepts","title":"OOP Concepts","summary":"Encapsulation, inheritance, polymorphism, and abstraction explained.","content":"<p>Object-Oriented Programming organizes code around objects that combine data and behavior.</p>\n<h4>The Four Pillars</h4>\n<ul>\n<li><b>Encapsulation</b> — Bundle data with methods that operate on it. Hide internal details using <code>private</code> fields and public getters/setters.</li>\n<li><b>Inheritance</b> — A child class (<code>extends</code>) reuses and extends a parent class. Example: <code>class Dog extends Animal</code>.</li>\n<li><b>Polymorphism</b> — One interface, many forms. A parent reference can point to any child object: <code>Animal a = new Dog();</code></li>\n<li><b>Abstraction</b> — Expose only what is necessary. Achieved via abstract classes and interfaces.</li>\n</ul>\n<div class=\"code-example\">public class Animal {\n    public void speak() { System.out.println(\"...\"); }\n}\n\npublic class Cat extends Animal {\n    @Override\n    public void speak() { System.out.println(\"Meow!\"); }\n}</div>\n<p class=\"note-tip\"><b>Tip:</b> Favor <b>composition</b> (\"has-a\") over deep inheritance hierarchies (\"is-a\") when designing flexible systems.</p>"},{"id":"Arrays","title":"Arrays","summary":"Fixed-size collections, indexing, iteration, and common array operations.","content":"<p>An array stores a <b>fixed number</b> of elements of the <b>same type</b>, accessed by a zero-based index.</p>\n<h4>Creating Arrays</h4>\n<div class=\"code-example\">int[] scores = new int[3];       // [0, 0, 0]\nscores[0] = 95;\n\nint[] primes = {2, 3, 5, 7, 11}; // shorthand initializer\nString[] names = new String[]{\"Ann\", \"Bob\"};</div>\n<h4>Common Operations</h4>\n<ul>\n<li><b>Length:</b> <code>scores.length</code> (not a method — no parentheses).</li>\n<li><b>Iterate:</b> use a for loop or enhanced for-each.</li>\n<li><b>Sort:</b> <code>Arrays.sort(scores);</code> (java.util.Arrays).</li>\n</ul>\n<h4>Multi-dimensional</h4>\n<div class=\"code-example\">int[][] grid = {{1, 2}, {3, 4}};\nSystem.out.println(grid[1][0]);  // 3</div>\n<p class=\"note-tip\"><b>Tip:</b> Array size is fixed at creation. If you need to grow or shrink a collection, use <code>ArrayList</code> instead.</p>"},{"id":"Methods","title":"Methods","summary":"Reusable blocks of logic — parameters, return types, and overloading.","content":"<p>A method is a named block of code that runs only when called. Methods belong to a class and can accept inputs and return outputs.</p>\n<h4>Method Anatomy</h4>\n<div class=\"code-example\">public static int add(int a, int b) {\n    return a + b;\n}\n\n// access   return  name   parameters\n// modifier  type</div>\n<h4>Calling Methods</h4>\n<div class=\"code-example\">int sum = add(4, 5);          // static method\nCalculator calc = new Calculator();\nint product = calc.multiply(3, 4);  // instance method</div>\n<h4>Method Overloading</h4>\n<p>Multiple methods with the <b>same name</b> but <b>different parameter lists</b>:</p>\n<div class=\"code-example\">void print(int x)    { System.out.println(x); }\nvoid print(String s) { System.out.println(s); }</div>\n<h4>Key Concepts</h4>\n<ul>\n<li><code>void</code> — method returns nothing.</li>\n<li><code>static</code> — belongs to the class, not an instance.</li>\n<li><b>Pass by value</b> — Java copies argument values; primitives copy the value, objects copy the reference.</li>\n</ul>\n<p class=\"note-tip\"><b>Tip:</b> Keep methods short and focused on one task. If a method exceeds ~20 lines, consider splitting it.</p>"},{"id":"Access Modifiers","title":"Access Modifiers","summary":"Control visibility with public, private, protected, and package-private.","content":"<p>Access modifiers control <b>who can see and use</b> classes, fields, and methods.</p>\n<h4>The Four Levels</h4>\n<ul>\n<li><code>public</code> — accessible from anywhere.</li>\n<li><code>protected</code> — accessible within the same package and by subclasses (even in other packages).</li>\n<li><em>package-private</em> (no keyword) — accessible only within the same package.</li>\n<li><code>private</code> — accessible only within the declaring class.</li>\n</ul>\n<div class=\"code-example\">public class BankAccount {\n    private double balance;   // hidden from outside\n\n    public double getBalance() {\n        return balance;       // controlled access\n    }\n\n    public void deposit(double amount) {\n        if (amount > 0) balance += amount;\n    }\n}</div>\n<h4>Why It Matters</h4>\n<p>Restricting access is the foundation of <b>encapsulation</b>. Expose only what callers need; keep implementation details private so you can change them without breaking other code.</p>\n<p class=\"note-tip\"><b>Tip:</b> Default to <code>private</code> for fields. Add <code>public</code> getters/setters only when external access is truly needed.</p>"},{"id":"Interfaces","title":"Interfaces","summary":"Define contracts that classes implement — multiple inheritance of type.","content":"<p>An interface defines a <b>contract</b> — a list of methods that implementing classes must provide. Java classes can <code>implements</code> multiple interfaces.</p>\n<h4>Defining &amp; Implementing</h4>\n<div class=\"code-example\">public interface Drawable {\n    void draw();                    // implicitly public &amp; abstract\n    default void highlight() {       // default method (Java 8+)\n        System.out.println(\"Highlighted!\");\n    }\n}\n\npublic class Circle implements Drawable {\n    @Override\n    public void draw() {\n        System.out.println(\"Drawing a circle\");\n    }\n}</div>\n<h4>Interfaces vs Abstract Classes</h4>\n<ul>\n<li>Interfaces: multiple inheritance of <b>type</b>, no state (until Java 8 default methods).</li>\n<li>Abstract classes: single inheritance, can have fields and partial implementations.</li>\n</ul>\n<h4>Common Uses</h4>\n<ul>\n<li>Define APIs that multiple unrelated classes share (<code>Comparable</code>, <code>Runnable</code>).</li>\n<li>Enable polymorphism: <code>Drawable shape = new Circle();</code></li>\n</ul>\n<p class=\"note-tip\"><b>Tip:</b> Use interfaces to describe <b>what</b> something can do; use classes to describe <b>what</b> something is.</p>"},{"id":"Exceptions","title":"Exceptions","summary":"try/catch/finally, checked vs unchecked, and throwing custom errors.","content":"<p>Exceptions are events that disrupt normal program flow. Java's exception model separates <b>checked</b> (must handle) and <b>unchecked</b> (runtime) exceptions.</p>\n<h4>try / catch / finally</h4>\n<div class=\"code-example\">try {\n    int result = 10 / 0;\n} catch (ArithmeticException e) {\n    System.out.println(\"Cannot divide by zero: \" + e.getMessage());\n} finally {\n    System.out.println(\"This always runs.\");\n}</div>\n<h4>Checked vs Unchecked</h4>\n<ul>\n<li><b>Checked</b> — <code>IOException</code>, <code>SQLException</code>. Compiler forces a <code>try-catch</code> or <code>throws</code> declaration.</li>\n<li><b>Unchecked</b> — <code>NullPointerException</code>, <code>ArrayIndexOutOfBoundsException</code>. Extend <code>RuntimeException</code>.</li>\n</ul>\n<h4>Throwing Exceptions</h4>\n<div class=\"code-example\">public void setAge(int age) {\n    if (age < 0) {\n        throw new IllegalArgumentException(\"Age cannot be negative\");\n    }\n    this.age = age;\n}</div>\n<p class=\"note-tip\"><b>Tip:</b> Catch specific exceptions first, not a broad <code>catch (Exception e)</code>. Log the message and fail gracefully.</p>"},{"id":"String Handling","title":"String Handling","summary":"Immutable strings, common methods, and StringBuilder for performance.","content":"<p><code>String</code> is one of Java's most-used classes. Strings are <b>immutable</b> — any \"change\" creates a new object.</p>\n<h4>Creating Strings</h4>\n<div class=\"code-example\">String s1 = \"Hello\";              // string literal (pooled)\nString s2 = new String(\"Hello\");  // heap object (avoid when possible)\nString s3 = s1 + \" World\";        // concatenation creates a new String</div>\n<h4>Essential Methods</h4>\n<ul>\n<li><code>length()</code> — number of characters.</li>\n<li><code>charAt(i)</code> — character at index <code>i</code>.</li>\n<li><code>substring(start, end)</code> — extract a portion.</li>\n<li><code>equals(other)</code> — compare content (never use <code>==</code> for content).</li>\n<li><code>toLowerCase()</code> / <code>toUpperCase()</code> — case conversion.</li>\n<li><code>trim()</code> — remove leading/trailing whitespace.</li>\n<li><code>split(regex)</code> — break into a <code>String[]</code>.</li>\n</ul>\n<h4>StringBuilder (mutable)</h4>\n<div class=\"code-example\">StringBuilder sb = new StringBuilder();\nfor (int i = 0; i < 1000; i++) {\n    sb.append(i).append(\", \");\n}\nString result = sb.toString();</div>\n<p class=\"note-tip\"><b>Tip:</b> Use <code>StringBuilder</code> inside loops for concatenation. Using <code>+</code> in a loop creates thousands of throwaway String objects.</p>"},{"id":"Collections","title":"Collections","summary":"List, Set, Map, and Queue — the Java Collections Framework overview.","content":"<p>The Java Collections Framework provides ready-made data structures for storing and manipulating groups of objects.</p>\n<h4>Core Interfaces</h4>\n<ul>\n<li><code>List</code> — ordered, allows duplicates. Implementations: <code>ArrayList</code>, <code>LinkedList</code>.</li>\n<li><code>Set</code> — no duplicates. Implementations: <code>HashSet</code>, <code>TreeSet</code>.</li>\n<li><code>Queue</code> — FIFO processing. Implementation: <code>LinkedList</code>, <code>PriorityQueue</code>.</li>\n<li><code>Map</code> — key-value pairs (not a Collection). Implementations: <code>HashMap</code>, <code>TreeMap</code>.</li>\n</ul>\n<div class=\"code-example\">List&lt;String&gt; names = new ArrayList&lt;&gt;();\nnames.add(\"Alice\");\nnames.add(\"Bob\");\n\nMap&lt;String, Integer&gt; ages = new HashMap&lt;&gt;();\nages.put(\"Alice\", 30);\nages.put(\"Bob\", 25);\n\nfor (String name : names) {\n    System.out.println(name + \": \" + ages.get(name));\n}</div>\n<h4>Choosing the Right Structure</h4>\n<ul>\n<li>Fast random access → <code>ArrayList</code></li>\n<li>Frequent insert/delete in middle → <code>LinkedList</code></li>\n<li>Unique elements, fast lookup → <code>HashSet</code></li>\n<li>Key-value lookups → <code>HashMap</code></li>\n</ul>\n<p class=\"note-tip\"><b>Tip:</b> Program to the interface (<code>List</code>, <code>Map</code>) not the implementation — it makes code easier to swap and test.</p>"},{"id":"Generics","title":"Generics","summary":"Parameterized types for compile-time safety and reusable data structures.","content":"<p>Generics add <b>type parameters</b> to classes and methods so the compiler can catch type errors at compile time instead of runtime.</p>\n<h4>Why Generics?</h4>\n<p>Before generics, collections stored <code>Object</code> and required casting:</p>\n<div class=\"code-example\">// Without generics (legacy — avoid)\nList list = new ArrayList();\nlist.add(\"hello\");\nString s = (String) list.get(0);  // risky cast\n\n// With generics (type-safe)\nList&lt;String&gt; words = new ArrayList&lt;&gt;();\nwords.add(\"hello\");\nString s = words.get(0);  // no cast needed</div>\n<h4>Generic Class Example</h4>\n<div class=\"code-example\">public class Box&lt;T&gt; {\n    private T content;\n\n    public void set(T content) { this.content = content; }\n    public T get() { return content; }\n}\n\nBox&lt;Integer&gt; intBox = new Box&lt;&gt;();\nintBox.set(42);</div>\n<h4>Bounded Type Parameters</h4>\n<div class=\"code-example\">public &lt;T extends Comparable&lt;T&gt;&gt; T findMax(T a, T b) {\n    return a.compareTo(b) > 0 ? a : b;\n}</div>\n<p class=\"note-tip\"><b>Tip:</b> Use generics everywhere you work with collections. The diamond operator <code>&lt;&gt;</code> (Java 7+) lets the compiler infer types.</p>"},{"id":"JVM and Bytecode","title":"JVM & Bytecode","summary":"How source code becomes bytecode and runs on the Java Virtual Machine.","content":"<p>Java's \"Write Once, Run Anywhere\" slogan is powered by the compilation pipeline and the Java Virtual Machine.</p>\n<h4>The Pipeline</h4>\n<ul>\n<li><b>.java source</b> — human-readable code you write.</li>\n<li><b>javac compiler</b> — translates source into <b>.class bytecode</b>.</li>\n<li><b>JVM</b> — loads bytecode, verifies it, and executes it via the interpreter or JIT compiler.</li>\n</ul>\n<div class=\"code-example\">// Hello.java\npublic class Hello {\n    public static void main(String[] args) {\n        System.out.println(\"Hello, JVM!\");\n    }\n}\n\n// Compile:  javac Hello.java  →  Hello.class\n// Run:      java Hello</div>\n<h4>Key Components</h4>\n<ul>\n<li><b>JRE</b> (Java Runtime Environment) — JVM + core libraries needed to run apps.</li>\n<li><b>JDK</b> (Java Development Kit) — JRE + development tools (<code>javac</code>, <code>jar</code>, debugger).</li>\n<li><b>JIT Compiler</b> — optimizes hot bytecode paths to native machine code at runtime.</li>\n<li><b>Garbage Collector</b> — automatically reclaims memory from unused objects.</li>\n</ul>\n<p class=\"note-tip\"><b>Tip:</b> Bytecode is platform-independent; only the JVM needs to be installed for your OS. That is why the same <code>.jar</code> runs on Windows, macOS, and Linux.</p>"},{"id":"File I/O","title":"File I/O","summary":"Read and write files with File, BufferedReader, and try-with-resources.","content":"<p>Java provides several APIs for reading and writing files. Modern code favors the NIO.2 API and try-with-resources.</p>\n<h4>Reading a Text File</h4>\n<div class=\"code-example\">import java.nio.file.*;\nimport java.util.List;\n\nPath path = Paths.get(\"data.txt\");\nList&lt;String&gt; lines = Files.readAllLines(path);\n\nfor (String line : lines) {\n    System.out.println(line);\n}</div>\n<h4>Writing a Text File</h4>\n<div class=\"code-example\">Path out = Paths.get(\"output.txt\");\nFiles.writeString(out, \"Hello, File I/O!\");</div>\n<h4>try-with-resources</h4>\n<p>Automatically closes resources (files, streams) even if an exception occurs:</p>\n<div class=\"code-example\">try (BufferedReader reader = Files.newBufferedReader(path)) {\n    String line;\n    while ((line = reader.readLine()) != null) {\n        System.out.println(line);\n    }\n} catch (IOException e) {\n    System.err.println(\"Error reading file: \" + e.getMessage());\n}</div>\n<p class=\"note-tip\"><b>Tip:</b> Always use try-with-resources for <code>Closeable</code> objects. Never rely on manual <code>close()</code> in a finally block.</p>"},{"id":"Multithreading","title":"Multithreading","summary":"Concurrent execution with Thread, Runnable, and synchronization basics.","content":"<p>Multithreading lets a program run multiple tasks <b>concurrently</b>, improving responsiveness and CPU utilization on multi-core machines.</p>\n<h4>Creating a Thread</h4>\n<div class=\"code-example\">// Option 1: extend Thread\nclass MyThread extends Thread {\n    public void run() {\n        System.out.println(\"Running in \" + getName());\n    }\n}\nnew MyThread().start();\n\n// Option 2: implement Runnable (preferred)\nRunnable task = () -> System.out.println(\"Runnable task\");\nnew Thread(task).start();</div>\n<h4>Synchronization</h4>\n<p>When multiple threads access shared data, use <code>synchronized</code> or <code>java.util.concurrent</code> utilities to prevent race conditions:</p>\n<div class=\"code-example\">private int count = 0;\n\npublic synchronized void increment() {\n    count++;\n}</div>\n<h4>Executor Framework</h4>\n<div class=\"code-example\">ExecutorService pool = Executors.newFixedThreadPool(4);\npool.submit(() -> System.out.println(\"Task on pool thread\"));\npool.shutdown();</div>\n<p class=\"note-tip\"><b>Tip:</b> Prefer <code>ExecutorService</code> over manually creating <code>Thread</code> objects. It manages thread lifecycle and pooling for you.</p>"}],"quiz":[{"id":"q1","text":"Which company developed Java?","html":false,"options":[{"value":"a","label":"Microsoft"},{"value":"b","label":"Sun Microsystems"},{"value":"c","label":"Google"}],"answer":"b","correctText":"Sun Microsystems","explanation":"Java was originally developed by James Gosling at Sun Microsystems in 1995."},{"id":"q2","text":"Which keyword is used for inheritance?","html":false,"options":[{"value":"a","label":"implement"},{"value":"b","label":"inherit"},{"value":"c","label":"extends"}],"answer":"c","correctText":"extends","explanation":"A subclass inherits from a parent class using the extends keyword."},{"id":"q3","text":"JVM stands for?","html":false,"options":[{"value":"a","label":"Java Virtual Machine"},{"value":"b","label":"Java Variable Method"},{"value":"c","label":"Java Version Manager"}],"answer":"a","correctText":"Java Virtual Machine","explanation":"The JVM executes compiled Java bytecode on any supported platform."},{"id":"q4","text":"Which of these is not a primitive data type?","html":false,"options":[{"value":"a","label":"int"},{"value":"b","label":"String"},{"value":"c","label":"boolean"}],"answer":"b","correctText":"String","explanation":"String is a reference type (a class), not one of Java's eight primitive types."},{"id":"q5","text":"What is the size of an int variable?","html":false,"options":[{"value":"a","label":"8 bit"},{"value":"b","label":"16 bit"},{"value":"c","label":"32 bit"}],"answer":"c","correctText":"32 bit","explanation":"A Java int is a 32-bit signed integer, ranging from about -2.1 billion to 2.1 billion."},{"id":"q6","text":"Which access modifier makes a member visible only within its own class?","html":false,"options":[{"value":"a","label":"public"},{"value":"b","label":"private"},{"value":"c","label":"protected"}],"answer":"b","correctText":"private","explanation":"private members are only accessible within the class that declares them — the core of encapsulation."},{"id":"q7","text":"Which collection does NOT allow duplicate elements?","html":false,"options":[{"value":"a","label":"ArrayList"},{"value":"b","label":"HashSet"},{"value":"c","label":"LinkedList"}],"answer":"b","correctText":"HashSet","explanation":"Set implementations like HashSet enforce uniqueness; List implementations allow duplicates."},{"id":"q8","text":"What does the <code>final</code> keyword mean when applied to a variable?","html":true,"options":[{"value":"a","label":"The variable cannot be reassigned"},{"value":"b","label":"The variable is hidden from other classes"},{"value":"c","label":"The variable is automatically static"}],"answer":"a","correctText":"The variable cannot be reassigned","explanation":"A final variable must be assigned once and cannot be pointed to a different value afterward."}]};
